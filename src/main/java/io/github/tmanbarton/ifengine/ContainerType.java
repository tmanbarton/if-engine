package io.github.tmanbarton.ifengine;

/**
 * Enum representing the type of container and its behavior.
 */
public enum ContainerType {
  /**
   * Inventory containers keep items in the player's inventory.
   */
  INVENTORY,

  /**
   * Location containers place items at the current location.
   */
  LOCATION
}